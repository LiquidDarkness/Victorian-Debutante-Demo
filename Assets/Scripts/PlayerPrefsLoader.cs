using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerPrefsLoader : MonoBehaviour
{
    /* PRÓBA 1 - NIEUDANA
    public Toggle fullscreenBool { set => PlayerPrefs.GetInt(windowMode.PrefsKey); }
    public TypeDistinguisher windowMode;

    [ContextMenu("Test")]
    public void Test()
    {
       // fullscreenBool
            //Equals(PlayerPrefs.GetInt(windowMode.PrefsKey));
    }
    */


}
